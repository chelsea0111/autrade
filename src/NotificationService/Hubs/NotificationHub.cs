using Microsoft.AspNetCore.SignalR;

namespace NotificationService.Hubs;

// when consuming events from service bus, then we can inject and make use of this hub
public class NotificationHub: Hub
{
    
}